{
  "name": "zotero-reader-tool-shortcuts",
  "version": "1.0.4",
  "private": true,
  "description": "Configurable Zotero Reader annotation tool shortcuts",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "npm test && python scripts/build_xpi.py"
  }
}
